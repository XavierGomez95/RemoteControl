import 'package:flutter/material.dart';

class RobotDetailsScreen extends StatelessWidget {
  const RobotDetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // Implementar UI basada en la Pantalla 2 del diseño
    return Scaffold(
      appBar: AppBar(
        title: const Text('Robot Details'),
      ),
      body: const Center(
        child: Text('Robot Details Screen Placeholder'),
      ),
    );
  }
}
